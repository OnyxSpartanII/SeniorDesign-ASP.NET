@model IEnumerable<SeniorDesignWebApp.victim>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Minor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Foreigner)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Female)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Minor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Foreigner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Female)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.VictimsId }) |
                @Html.ActionLink("Details", "Details", new { id=item.VictimsId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.VictimsId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
